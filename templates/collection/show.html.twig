{% extends 'base.html.twig' %}

{% block title %}Collection{% endblock %}

{% block body %}
    <h1>Collection</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ collection.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ collection.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_collection_index') }}">back to list</a>

    <a href="{{ path('app_collection_edit', {'id': collection.id}) }}">edit</a>

    {{ include('collection/_delete_form.html.twig') }}
{% endblock %}
