{% extends 'base.html.twig' %}

{% block title %}Collection{% endblock %}

{% block body %}
    <h1>Details de la collection</h1>

<div class="w-full flex flex-col ">
        <div class="bg-lightenDarkBlue w-1/2 p-4 mt-4 self-center">
            <h2 class="text-2xl">{{ collection.name }}</h2>
            <p>{{ collection.category.name }}</p>
            <div class="mt-4">
                <a href="{{ path('app_collection_edit', {'id': collection.id}) }}" class="ml-4">Editer</a>
                <a href="{{ path('app_collection_delete', {'id' : collection.id}) }}" class="ml-4">Supprimer</a>
            </div>
        </div>
</div>
<a href="{{ path('app_collection_index') }}" class="ml-4">Retour arrière (page des collections)</a>

{% endblock %}
